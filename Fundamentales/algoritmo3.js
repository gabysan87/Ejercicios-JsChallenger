// Obtener tipo de valor

// Escribe una función que tome un valor como argumento. Devuelve el tipo del valor.

const myFunction = (a) => {
    
    return typeof a;
   
}

console.log(myFunction(1))

// el operador typeof devuelve el tipo del valor que contiene la propiedad